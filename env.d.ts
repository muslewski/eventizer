// Committed counterpart of the generated (gitignored) next-env.d.ts so that
// `tsc --noEmit` works in environments that never ran `next dev`/`next build`
// (e.g. CI). Keeps static asset imports (@/assets/*.png …) typed.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
